//to calculate the cubic value of the number
#include <stdio.h>
void main()
{
    int num,value;
    printf("enter the number:");
    scanf("%d",&num);
    value=num*num*num;
    printf("the cube value  for the given number is %d",value);
}
